# الأسرار — لا تدخل الصورة أبداً
.env
.env.*

# البيانات تعيش في حجم، لا في الصورة
data/

# لا لزوم لها في الإنتاج
test/
node_modules/
# القفل يدخل: npm ci يحتاجه
!package-lock.json
*.md
!README.md
.git/
.gitignore
.vscode/
.idea/
Thumbs.db
Desktop.ini
.DS_Store


# الوثائق مرجع للبشر لا للحاوية
docs/

# ناتج بناء Next: يُبنى داخل الصورة، ونسخ نسخة المضيف تُفسده
.next/

# أصول التصميم الخام (٣٣ ميغا) — تُعالَج على الجهاز لا في الصورة
assets-src/

# إعدادات المحرّر والوكيل
.claude/
