/**
 * عنوان الخادم الذي تُوجَّه إليه الفحوص.
 *
 * كان `http://localhost:3000` مكتوباً في ثلاثة عشر ملفاً. والمنفذ
 * ٣٠٠٠ منفذ شائع: أي مشروع آخر يعمل على الجهاز يستطيع أن يحجزه
 * — وويندوز يسمح بربطين على المنفذ نفسه، فيستلم الرابطُ الأخير
 * الطلبات. عندها تصل فحوصنا إلى تطبيق غريب تماماً، فتفشل بأخطاء
 * لا معنى لها (`data` فارغة، أو «fetch failed») ولا تدلّ على شيء.
 *
 * لذلك صار العنوان مضبوطاً من البيئة:
 *
 *     PORT=3100 npm start
 *     RVIOS_TEST_URL=http://localhost:3100 npm test
 *
 * والافتراضي يبقى ٣٠٠٠ فلا يتغيّر شيء لمن لا يحتاج غيره.
 */
/**
 * ★ الوجهة صارت تطبيق Next لا الخادم القديم.
 *
 * بعد ترحيل الدخول والتسجيل واللوحة، لم يعد خادمٌ واحد يخدم كل
 * شيء: Next يقف في المقدّمة ويمرّر إليه ما لم يُرحَّل (`/api`
 * و`/assets` و`/admin` و`/track`). فتوجيه الفحوص إلى المنفذ
 * القديم صار يفحص **نصف النظام** — ويمرّ بينما الصفحة التي
 * يفتحها الزائر فعلاً معطوبة.
 *
 *     npm run dev                      # الاثنان معاً
 *     RVIOS_TEST_URL=http://localhost:3001 npm run test:suites
 */
export const BASE = (process.env.RVIOS_TEST_URL || 'http://localhost:3001').replace(/\/+$/, '');
