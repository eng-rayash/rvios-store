# تقرير جاهزية الرفع — RVIOS Store

> مبنيّ على تدقيق آلي للكود لا على تقدير. أُعيد توليده بتشغيل
> `node test/readiness.mjs` بعد أي تغيير جوهري.

**الحالة:** ١٨٠ فحصاً · صفر فشل · ١٩ جدولاً · ٥٣ مسار API · ٦٢ ملفاً

---

## الحكم

**جاهز للنشر التجريبي · غير جاهز للإطلاق العام.**

المنتج مكتمل وظيفياً ومُختبَر: التاجر ينشئ متجره ويبيع، والعميل يطلب ويتتبّع،
والإدارة تراقب. لكن ثلاثة عوائق تمنع فتح الباب للجمهور.

| | العدد |
|---|---|
| بنود مكتملة ومُختبَرة | ٢٧ |
| عوائق تمنع الإطلاق العام | ٣ |
| بنود للتشغيل الآمن | ٦ |
| بنود مؤجَّلة بوعي | ٩ |

---

## ١. ما يمنع الإطلاق العام

### ١.١ الفوترة بلا واجهة 🔴

نظام الفوترة مبنيّ كاملاً في [`src/billing.js`](src/billing.js) — فواتير بمرجع
`RV-INV-####`، رفع إيصال يفعّل الباقة فوراً، طابور مراجعة، سجل تدقيق —
و**٣٤ فحصاً تمر عليه**. لكن **لا يوجد أي عنصر واجهة يستدعيه**.

النتيجة: التاجر لا يستطيع الدفع، والإدارة لا ترى طابور الإيصالات الذي وصفته
المواصفة بأنه «أكثر أقسام لوحة الإدارة استخداماً» (§٥.٦).

**المطلوب:** قسم فوترة في [`public/dashboard.html`](public/dashboard.html)،
وقسم مراجعة مدفوعات في [`public/admin.html`](public/admin.html).

### ١.٢ كلمة مرور الإدارة الافتراضية غير محجوبة 🔴

`rvios-admin` مكتوبة كقيمة افتراضية ولا شيء يمنع النشر بها.
الفحص الذي يوقف الإقلاع موجود في [`src/config.js`](src/config.js) لكنه
**غير مفعَّل**: `assertConfig()` لم يُستدعَ، و[`src/auth.js`](src/auth.js)
يقرأ `process.env` مباشرة.

### ١.٣ لا نسخ احتياطي 🟠

`data/` يحوي قاعدة البيانات وكل صور المتاجر. عطب قرص = المنصة كلها.
المطلوب: `VACUUM INTO` مجدول مع تدوير، وتصدير **إلى مكان مستقل عن الخادم** (§٦.٧).

---

## ٢. ما يلزم للتشغيل الآمن

| البند | الحالة | الأثر |
|---|---|---|
| ربط `config.js` و`logger.js` | مكتوب وغير مربوط | كود ميت الآن |
| نقطة صحة `/health` | مفقود | لا وسيلة لمعرفة أن الخدمة حية |
| إيقاف رشيق | مفقود | إعادة النشر تقطع الطلبات الجارية |
| ضغط gzip/brotli | مفقود | مؤثر جداً على الشبكات الضعيفة |
| ترحيلات مرقّمة | DDL مباشر في `db.js` | تغيير المخطط يدوي وغير قابل للتراجع |
| Dockerfile · وكيل عكسي · CI | مفقود | النشر يدوي والاختبارات لا تعمل تلقائياً |

---

## ٣. مؤجَّل بوعي

| البند | المرجع | لماذا يمكن الانتظار |
|---|---|---|
| **متجر إضافي لباقة برو** | §٢.١ | ⚠️ **وعد معلن في صفحة الأسعار** — يُبنى أو يُزال |
| توثيق الهوية KYC | §٤ | التوثيق يدوي من الإدارة؛ يكفي لعشرات المتاجر الأولى |
| تسلسل قنوات إرسال بديلة | §٢.٥ | مزوّد واحد يكفي للبداية |
| قياس نجاح كل قناة | §٢.٥ | بلا قنوات متعددة لا معنى للمقارنة |
| تذكيرات D-7 / D-1 | §٥.٥ | المهلة تعمل؛ التذكير تحسين |
| تجميد ٢٤ ساعة عند تغيير الرقم | §٧-٥ | لا مسار لتغيير الرقم أصلاً |
| PWA | §٦.٥ | الموقع يعمل على الهاتف |
| تحويل WebP على الخادم | §٥.٣ | الضغط في المتصفح يعمل، لكنه أثقل |
| ترقيم في لوحة التاجر | — | حد ثابت ٢٠٠ صف يكفي حالياً |

---

## ٤. ما بُني ومُختبَر

**الأمان** — تجزئة HMAC للرمز · مقارنة ثابتة الزمن · منع تعداد الأرقام ·
مهلة إعادة الإرسال · حد على الرقم والـIP · ترويسات CSP · كوكي Secure · سجل تدقيق

**العزل** — بوابة واحدة لبيانات المستأجرين · رفض SQL بلا `store_id` ·
فحص عند الإقلاع · [DDL جاهز لـPostgres RLS](ops/postgres-rls.sql)

**تدفق الطلب** — تسجيل قبل واتساب · حجز مخزون في معاملة · رسوم توصيل ·
صفحة تتبّع · إشعار التاجر

**الروابط** — حرفنة عربية→لاتينية · سجل الروابط القديمة · تحويل ٣٠١ ·
حماية الرابط المتقاعد

**الاكتشاف** — HTML من الخادم · Open Graph · Schema.org · sitemap و robots

**الفوترة (خادم)** — اشتراكات وفواتير · تفعيل فوري · سحب عند الرفض ·
إخفاء لا حذف · أسعار في قاعدة البيانات

---

## ٥. الترتيب الموصى به

1. **واجهتا الفوترة** — يفتح الإيراد، والخادم جاهز بالكامل
2. **ربط الإعدادات والتسجيل** — يغلق ثغرة كلمة المرور
3. **النسخ الاحتياطي** — قبل أول تاجر حقيقي
4. **طبقة التشغيل** — health · إيقاف رشيق · ضغط · Docker · CI
5. **حسم وعد «متجر إضافي»** — بناء أو إزالة

---

## ٦. قرارات تنتظر صاحب المشروع

| القرار | لماذا يهم |
|---|---|
| **توثيق Meta Business** | الكود جاهز وينتظر مفتاحين. الاعتماد يستغرق أياماً — **ابدأه اليوم بالتوازي** |
| **أسعار الباقات** | تُضبط من لوحة الإدارة. الفوترة ترفض العمل قبل إعلانها |
| **وسيلة التحصيل** | لا تظهر للتاجر ما لم تُضبط تعليماتها. تحقّق من قيود حسابك أولاً |
| **توقيت الانتقال لـPostgres** | المحفّز: خادم ثانٍ أو مطوّر ثانٍ |

---

*انظر [ARCHITECTURE.md](ARCHITECTURE.md) لتفصيل ما اعتُمد من المواصفة وما اختُلف فيه ولماذا.*
