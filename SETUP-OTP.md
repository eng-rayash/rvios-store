# تفعيل التحقق برقم الجوال

المنصة تدعم ثلاثة مزوّدات، يُختار بينها بمتغيّر بيئة واحد `OTP_PROVIDER`.
**لا يتغيّر أي كود** عند التبديل — نقطة التبديل كلها في [`src/notify.js`](src/notify.js).

```bash
OTP_PROVIDER=console    # الافتراضي — للتطوير
OTP_PROVIDER=whatsapp   # واتساب Cloud API  ← الموصى به
OTP_PROVIDER=sms        # أي بوابة SMS محلية
```

عند تشغيل الخادم يُطبع سطر يوضّح الحالة:

```
إرسال رمز التحقق: whatsapp ✓ مضبوط
```

---

## الخيار الأول: واتساب Cloud API — الموصى به

**لماذا هو الأنسب لليمن:** الانتشار شبه شامل، والتسليم أوثق بكثير من SMS الدولي
الذي كثيراً ما يُحجب أو يتأخر. والأهم: **مجاني** ضمن حدود شهرية واسعة،
ورسائل المصادقة ضمن فئة مسعّرة منخفضة.

### الخطوات

**١. أنشئ حساب مطوّر ونشاطاً تجارياً**

- ادخل إلى [developers.facebook.com](https://developers.facebook.com) وسجّل حساباً
- أنشئ تطبيقاً جديداً من نوع **Business**
- أضف منتج **WhatsApp** إلى التطبيق

**٢. اربط رقم المرسِل**

- من لوحة WhatsApp اختر *API Setup*
- أضف رقم هاتف للنشاط التجاري (يجب ألا يكون مسجّلاً على واتساب عادي)
- انسخ **Phone number ID** — هو `WHATSAPP_PHONE_ID`

**٣. أنشئ قالب المصادقة**

من *Message Templates* → *Create Template*:

| الحقل | القيمة |
|---|---|
| الاسم | `otp_code` |
| الفئة | **Authentication** |
| اللغة | العربية `ar` |
| النص | `رمز التحقق الخاص بك هو {{1}}` |
| الزر | Copy code |

> قوالب المصادقة تُعتمد عادة خلال دقائق، بخلاف القوالب التسويقية.

**٤. احصل على رمز دائم**

الرمز المؤقت في الواجهة ينتهي خلال ٢٤ ساعة. للإنتاج:
أنشئ *System User* من *Business Settings*، امنحه صلاحية التطبيق،
وولّد رمزاً دائماً بصلاحية `whatsapp_business_messaging`.

**٥. شغّل**

```bash
OTP_PROVIDER=whatsapp WHATSAPP_TOKEN=EAAG... WHATSAPP_PHONE_ID=123456789 npm start
```

### كل المتغيّرات

| المتغيّر | إلزامي | الافتراضي |
|---|---|---|
| `WHATSAPP_TOKEN` | ✔ | — |
| `WHATSAPP_PHONE_ID` | ✔ | — |
| `WHATSAPP_TEMPLATE` | — | `otp_code` |
| `WHATSAPP_LANG` | — | `ar` |

**فائدة إضافية:** حين يكون واتساب مفعّلاً، يصل التاجر **إشعار تلقائي بكل طلب
جديد** على واتساب — وهو ما يحل أخطر فجوة في المنصة (§٣.٣: سرعة رد التاجر).

---

## الخيار الثاني: بوابة SMS محلية

يناسب أي مزوّد يمني أو عربي يقبل طلب HTTP بسيط.

### مثال GET

```bash
OTP_PROVIDER=sms \
SMS_URL="https://sms.example.com/send?to={phone}&text={text}&key=YOUR_KEY" \
npm start
```

### مثال POST

```bash
OTP_PROVIDER=sms \
SMS_METHOD=POST \
SMS_URL="https://api.example.com/v1/messages" \
SMS_AUTH="Bearer YOUR_KEY" \
SMS_BODY='{"to":"{phone}","message":"{text}","sender":"RVIOS"}' \
npm start
```

### المتغيّرات

| المتغيّر | الوصف |
|---|---|
| `SMS_URL` | الرابط، ويقبل `{phone}` و`{code}` و`{text}` |
| `SMS_METHOD` | `GET` (افتراضي) أو `POST` |
| `SMS_BODY` | جسم الطلب لـ POST، بنفس المتغيّرات |
| `SMS_AUTH` | قيمة ترويسة `Authorization` |
| `SMS_TEXT` | نص الرسالة — افتراضياً `رمز التحقق في RVIOS Store: {code}` |

الرقم يُمرَّر بالصيغة الدولية بلا `+` (مثال: `967771234567`).

---

## الخيار الثالث: وضع التطوير

```bash
npm start        # OTP_PROVIDER=console افتراضياً
```

الرمز يظهر في مكانين:

1. **سجل الخادم** — `✉ رمز التحقق للرقم 967777000000 هو: 482913`
2. **صفحة التحقق نفسها** — سطر أخضر أسفل الحقول

> هذا الإظهار مقيَّد بشرطين معاً: `NODE_ENV !== 'production'` **و** المزوّد
> `console`. بمجرد ضبط مزوّد فعلي يختفي الرمز من الرد تلقائياً حتى لو بقيت
> في وضع التطوير — فلا يمكن أن يتسرّب في الإنتاج بالسهو.

---

## الأمان المطبَّق

| الإجراء | القيمة |
|---|---|
| طول الرمز | ٦ أرقام من `crypto.randomInt` |
| صلاحية الرمز | ٥ دقائق |
| محاولات خاطئة | ٥ ثم يُلغى الرمز |
| طلبات الرمز لكل رقم | ٣ كل ١٠ دقائق |
| طلبات الرمز لكل IP | ١٥ كل ١٠ دقائق |
| تخزين الرمز | يُحذف فور نجاح التحقق |
| فشل الإرسال | يُبلَّغ المستخدم بـ ٥٠٢ ولا يُبتلع صامتاً |

---

## اختبار سريع

```bash
curl -X POST http://localhost:3000/api/auth/request-code \
  -H "content-type: application/json" \
  -d '{"phone":"777123456"}'
```

- **وضع التطوير:** يعود `devCode` في الرد
- **مزوّد فعلي:** لا يعود `devCode`، وتصل الرسالة للجوال
- **إعداد ناقص:** يعود `502` مع سبب واضح في سجل الخادم
