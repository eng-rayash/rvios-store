# الأسرار — لا تُرفع أبداً
# مفتاح API مكشوف يعني استنزاف رصيد رسائلك خلال دقائق (§٦.٧)
.env
.env.*
!.env.example

# قاعدة البيانات والصور المرفوعة
data/

# Node
node_modules/
npm-debug.log*

# نظام التشغيل والمحررات
Thumbs.db
Desktop.ini
.DS_Store
.vscode/
.idea/

# ── Next ──────────────────────────────────────────────────
# ناتج البناء وذاكرته: يُعاد توليدهما، وحجمهما بالمئات
.next/
out/
*.tsbuildinfo
# يكتبه Next عند كل إقلاع ويشير إلى ملفات داخل .next
next-env.d.ts

# مهارات الوكيل المستوردة — ليست مصدر هذا المشروع
.claude/skills/

# مصادر التصميم الخام — ثقيلة وليست جزءاً من البناء
assets-src/
