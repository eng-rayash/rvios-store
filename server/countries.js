// ═══════════════════════════════════════════════════════════
//  الدول والعملات — إعادة تصدير لا نسخة
//
//  التطبيق الفعلي يعيش في `public/assets/js/countries.js` لأن
//  المتصفح يحتاجه أيضاً (صفحة المتجر واللوحة تعرضان العملة).
//  والخادم يستورده من هناك مباشرةً كما يفعل `src/render.js`
//  مع محرّك الألوان — فلا تُوجد نسختان على الخادم أصلاً.
//
//  تبقى نسخة ثالثة داخل `src/lib/` لأن Turbopack لا يستورد
//  من خارج جذر التطبيق، ويحرس تطابقها `npm run check:theme`
//  قبل كل بناء.
//
//  هذا الملف يبقى ليظل `./countries.js` هو ما تستورده وحدات
//  الخادم — مسار واحد قصير بدل تسلّق المجلدات في كل ملف.
// ═══════════════════════════════════════════════════════════
export {
  COUNTRIES, DEFAULT_COUNTRY, countryOf, symbolOf,
  toE164, validE164, countryOfPhone, localOf,
} from '../public/assets/js/countries.js';
